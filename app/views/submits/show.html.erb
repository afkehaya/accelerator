<div class="container">
	<p id="notice"><%= notice %></p>

	<p>
	  <strong>Name:</strong>
	  <%= @submit.name %>
	</p>

	<%= link_to 'Edit', edit_submit_path(@submit), class: "btn btn-primary btn-lg" %> |
	<%= link_to 'Back', submits_path, class: "btn btn-default btn-lg" %>
</div>
