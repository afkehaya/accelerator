<div class="container">
    <h1>Listing submits</h1>

    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @submits.each do |submit| %>
          <tr>
            <td><%= submit.name %></td>
            <td><%= link_to 'Show', submit %></td>
            <td><%= link_to 'Edit', edit_submit_path(submit) %></td>
            <td><%= link_to 'Destroy', submit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Application', new_submit_path, class: "btn btn-default btn-lg" %>
</div>
